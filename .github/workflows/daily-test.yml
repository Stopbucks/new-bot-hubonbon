name: Test Trigger (Discount Keyword)

on:
  schedule:
    # 台灣時間 15:50 = UTC 07:50 (注意下方縮排)
    - cron: '50 7 * * *'
  
  # 允許手動測試
  workflow_dispatch:

jobs:
  trigger-render:
    runs-on: ubuntu-latest
    steps:
      - name: Call Render Test Route
        run: |
          curl -X GET https://my-news-bot-77zr.onrender.com/test-trigger